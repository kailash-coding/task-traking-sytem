# Simple Task Tracker

Open `task-tracker.html` in your browser to run the lightweight task tracking app. Tasks are stored in `localStorage` under the key `simple-tasks-v1`.

Features:
- Add, edit, delete tasks
- Mark tasks complete
- Filter by All / Active / Completed
- Clear completed tasks
